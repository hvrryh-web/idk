# Frontend Environment Setup

- Use `.env.example` in `frontend/` as a template for local environment variables.
- Main variables:
  - `VITE_API_URL` (default: http://localhost:8000/api/v1)
- Never commit production secrets.

# ...existing code...
